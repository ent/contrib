// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/contrib/entproto/internal/entprototest/ent/predicate"
	"entgo.io/contrib/entproto/internal/entprototest/ent/servicewithid"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ServiceWithIDQuery is the builder for querying ServiceWithID entities.
type ServiceWithIDQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ServiceWithID
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServiceWithIDQuery builder.
func (swiq *ServiceWithIDQuery) Where(ps ...predicate.ServiceWithID) *ServiceWithIDQuery {
	swiq.predicates = append(swiq.predicates, ps...)
	return swiq
}

// Limit adds a limit step to the query.
func (swiq *ServiceWithIDQuery) Limit(limit int) *ServiceWithIDQuery {
	swiq.limit = &limit
	return swiq
}

// Offset adds an offset step to the query.
func (swiq *ServiceWithIDQuery) Offset(offset int) *ServiceWithIDQuery {
	swiq.offset = &offset
	return swiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (swiq *ServiceWithIDQuery) Unique(unique bool) *ServiceWithIDQuery {
	swiq.unique = &unique
	return swiq
}

// Order adds an order step to the query.
func (swiq *ServiceWithIDQuery) Order(o ...OrderFunc) *ServiceWithIDQuery {
	swiq.order = append(swiq.order, o...)
	return swiq
}

// First returns the first ServiceWithID entity from the query.
// Returns a *NotFoundError when no ServiceWithID was found.
func (swiq *ServiceWithIDQuery) First(ctx context.Context) (*ServiceWithID, error) {
	nodes, err := swiq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{servicewithid.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) FirstX(ctx context.Context) *ServiceWithID {
	node, err := swiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ServiceWithID ID from the query.
// Returns a *NotFoundError when no ServiceWithID ID was found.
func (swiq *ServiceWithIDQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = swiq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{servicewithid.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) FirstIDX(ctx context.Context) int32 {
	id, err := swiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ServiceWithID entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ServiceWithID entity is found.
// Returns a *NotFoundError when no ServiceWithID entities are found.
func (swiq *ServiceWithIDQuery) Only(ctx context.Context) (*ServiceWithID, error) {
	nodes, err := swiq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{servicewithid.Label}
	default:
		return nil, &NotSingularError{servicewithid.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) OnlyX(ctx context.Context) *ServiceWithID {
	node, err := swiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ServiceWithID ID in the query.
// Returns a *NotSingularError when more than one ServiceWithID ID is found.
// Returns a *NotFoundError when no entities are found.
func (swiq *ServiceWithIDQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = swiq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{servicewithid.Label}
	default:
		err = &NotSingularError{servicewithid.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := swiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ServiceWithIDs.
func (swiq *ServiceWithIDQuery) All(ctx context.Context) ([]*ServiceWithID, error) {
	if err := swiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return swiq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) AllX(ctx context.Context) []*ServiceWithID {
	nodes, err := swiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ServiceWithID IDs.
func (swiq *ServiceWithIDQuery) IDs(ctx context.Context) ([]int32, error) {
	var ids []int32
	if err := swiq.Select(servicewithid.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) IDsX(ctx context.Context) []int32 {
	ids, err := swiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (swiq *ServiceWithIDQuery) Count(ctx context.Context) (int, error) {
	if err := swiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return swiq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) CountX(ctx context.Context) int {
	count, err := swiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (swiq *ServiceWithIDQuery) Exist(ctx context.Context) (bool, error) {
	if err := swiq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return swiq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (swiq *ServiceWithIDQuery) ExistX(ctx context.Context) bool {
	exist, err := swiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServiceWithIDQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (swiq *ServiceWithIDQuery) Clone() *ServiceWithIDQuery {
	if swiq == nil {
		return nil
	}
	return &ServiceWithIDQuery{
		config:     swiq.config,
		limit:      swiq.limit,
		offset:     swiq.offset,
		order:      append([]OrderFunc{}, swiq.order...),
		predicates: append([]predicate.ServiceWithID{}, swiq.predicates...),
		// clone intermediate query.
		sql:    swiq.sql.Clone(),
		path:   swiq.path,
		unique: swiq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (swiq *ServiceWithIDQuery) GroupBy(field string, fields ...string) *ServiceWithIDGroupBy {
	grbuild := &ServiceWithIDGroupBy{config: swiq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := swiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return swiq.sqlQuery(ctx), nil
	}
	grbuild.label = servicewithid.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (swiq *ServiceWithIDQuery) Select(fields ...string) *ServiceWithIDSelect {
	swiq.fields = append(swiq.fields, fields...)
	selbuild := &ServiceWithIDSelect{ServiceWithIDQuery: swiq}
	selbuild.label = servicewithid.Label
	selbuild.flds, selbuild.scan = &swiq.fields, selbuild.Scan
	return selbuild
}

func (swiq *ServiceWithIDQuery) prepareQuery(ctx context.Context) error {
	for _, f := range swiq.fields {
		if !servicewithid.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if swiq.path != nil {
		prev, err := swiq.path(ctx)
		if err != nil {
			return err
		}
		swiq.sql = prev
	}
	return nil
}

func (swiq *ServiceWithIDQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ServiceWithID, error) {
	var (
		nodes = []*ServiceWithID{}
		_spec = swiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ServiceWithID).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ServiceWithID{config: swiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, swiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (swiq *ServiceWithIDQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := swiq.querySpec()
	_spec.Node.Columns = swiq.fields
	if len(swiq.fields) > 0 {
		_spec.Unique = swiq.unique != nil && *swiq.unique
	}
	return sqlgraph.CountNodes(ctx, swiq.driver, _spec)
}

func (swiq *ServiceWithIDQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := swiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (swiq *ServiceWithIDQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   servicewithid.Table,
			Columns: servicewithid.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt32,
				Column: servicewithid.FieldID,
			},
		},
		From:   swiq.sql,
		Unique: true,
	}
	if unique := swiq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := swiq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, servicewithid.FieldID)
		for i := range fields {
			if fields[i] != servicewithid.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := swiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := swiq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := swiq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := swiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (swiq *ServiceWithIDQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(swiq.driver.Dialect())
	t1 := builder.Table(servicewithid.Table)
	columns := swiq.fields
	if len(columns) == 0 {
		columns = servicewithid.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if swiq.sql != nil {
		selector = swiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if swiq.unique != nil && *swiq.unique {
		selector.Distinct()
	}
	for _, p := range swiq.predicates {
		p(selector)
	}
	for _, p := range swiq.order {
		p(selector)
	}
	if offset := swiq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := swiq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ServiceWithIDGroupBy is the group-by builder for ServiceWithID entities.
type ServiceWithIDGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (swigb *ServiceWithIDGroupBy) Aggregate(fns ...AggregateFunc) *ServiceWithIDGroupBy {
	swigb.fns = append(swigb.fns, fns...)
	return swigb
}

// Scan applies the group-by query and scans the result into the given value.
func (swigb *ServiceWithIDGroupBy) Scan(ctx context.Context, v any) error {
	query, err := swigb.path(ctx)
	if err != nil {
		return err
	}
	swigb.sql = query
	return swigb.sqlScan(ctx, v)
}

func (swigb *ServiceWithIDGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range swigb.fields {
		if !servicewithid.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := swigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := swigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (swigb *ServiceWithIDGroupBy) sqlQuery() *sql.Selector {
	selector := swigb.sql.Select()
	aggregation := make([]string, 0, len(swigb.fns))
	for _, fn := range swigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(swigb.fields)+len(swigb.fns))
		for _, f := range swigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(swigb.fields...)...)
}

// ServiceWithIDSelect is the builder for selecting fields of ServiceWithID entities.
type ServiceWithIDSelect struct {
	*ServiceWithIDQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (swis *ServiceWithIDSelect) Scan(ctx context.Context, v any) error {
	if err := swis.prepareQuery(ctx); err != nil {
		return err
	}
	swis.sql = swis.ServiceWithIDQuery.sqlQuery(ctx)
	return swis.sqlScan(ctx, v)
}

func (swis *ServiceWithIDSelect) sqlScan(ctx context.Context, v any) error {
	rows := &sql.Rows{}
	query, args := swis.sql.Query()
	if err := swis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
