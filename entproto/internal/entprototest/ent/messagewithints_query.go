// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/contrib/entproto/internal/entprototest/ent/messagewithints"
	"entgo.io/contrib/entproto/internal/entprototest/ent/predicate"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MessageWithIntsQuery is the builder for querying MessageWithInts entities.
type MessageWithIntsQuery struct {
	config
	ctx        *QueryContext
	order      []messagewithints.OrderOption
	inters     []Interceptor
	predicates []predicate.MessageWithInts
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MessageWithIntsQuery builder.
func (mwiq *MessageWithIntsQuery) Where(ps ...predicate.MessageWithInts) *MessageWithIntsQuery {
	mwiq.predicates = append(mwiq.predicates, ps...)
	return mwiq
}

// Limit the number of records to be returned by this query.
func (mwiq *MessageWithIntsQuery) Limit(limit int) *MessageWithIntsQuery {
	mwiq.ctx.Limit = &limit
	return mwiq
}

// Offset to start from.
func (mwiq *MessageWithIntsQuery) Offset(offset int) *MessageWithIntsQuery {
	mwiq.ctx.Offset = &offset
	return mwiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mwiq *MessageWithIntsQuery) Unique(unique bool) *MessageWithIntsQuery {
	mwiq.ctx.Unique = &unique
	return mwiq
}

// Order specifies how the records should be ordered.
func (mwiq *MessageWithIntsQuery) Order(o ...messagewithints.OrderOption) *MessageWithIntsQuery {
	mwiq.order = append(mwiq.order, o...)
	return mwiq
}

// First returns the first MessageWithInts entity from the query.
// Returns a *NotFoundError when no MessageWithInts was found.
func (mwiq *MessageWithIntsQuery) First(ctx context.Context) (*MessageWithInts, error) {
	nodes, err := mwiq.Limit(1).All(setContextOp(ctx, mwiq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{messagewithints.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) FirstX(ctx context.Context) *MessageWithInts {
	node, err := mwiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MessageWithInts ID from the query.
// Returns a *NotFoundError when no MessageWithInts ID was found.
func (mwiq *MessageWithIntsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mwiq.Limit(1).IDs(setContextOp(ctx, mwiq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{messagewithints.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) FirstIDX(ctx context.Context) int {
	id, err := mwiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MessageWithInts entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MessageWithInts entity is found.
// Returns a *NotFoundError when no MessageWithInts entities are found.
func (mwiq *MessageWithIntsQuery) Only(ctx context.Context) (*MessageWithInts, error) {
	nodes, err := mwiq.Limit(2).All(setContextOp(ctx, mwiq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{messagewithints.Label}
	default:
		return nil, &NotSingularError{messagewithints.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) OnlyX(ctx context.Context) *MessageWithInts {
	node, err := mwiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MessageWithInts ID in the query.
// Returns a *NotSingularError when more than one MessageWithInts ID is found.
// Returns a *NotFoundError when no entities are found.
func (mwiq *MessageWithIntsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mwiq.Limit(2).IDs(setContextOp(ctx, mwiq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{messagewithints.Label}
	default:
		err = &NotSingularError{messagewithints.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) OnlyIDX(ctx context.Context) int {
	id, err := mwiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MessageWithIntsSlice.
func (mwiq *MessageWithIntsQuery) All(ctx context.Context) ([]*MessageWithInts, error) {
	ctx = setContextOp(ctx, mwiq.ctx, ent.OpQueryAll)
	if err := mwiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MessageWithInts, *MessageWithIntsQuery]()
	return withInterceptors[[]*MessageWithInts](ctx, mwiq, qr, mwiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) AllX(ctx context.Context) []*MessageWithInts {
	nodes, err := mwiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MessageWithInts IDs.
func (mwiq *MessageWithIntsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mwiq.ctx.Unique == nil && mwiq.path != nil {
		mwiq.Unique(true)
	}
	ctx = setContextOp(ctx, mwiq.ctx, ent.OpQueryIDs)
	if err = mwiq.Select(messagewithints.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) IDsX(ctx context.Context) []int {
	ids, err := mwiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mwiq *MessageWithIntsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mwiq.ctx, ent.OpQueryCount)
	if err := mwiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mwiq, querierCount[*MessageWithIntsQuery](), mwiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) CountX(ctx context.Context) int {
	count, err := mwiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mwiq *MessageWithIntsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mwiq.ctx, ent.OpQueryExist)
	switch _, err := mwiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mwiq *MessageWithIntsQuery) ExistX(ctx context.Context) bool {
	exist, err := mwiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MessageWithIntsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mwiq *MessageWithIntsQuery) Clone() *MessageWithIntsQuery {
	if mwiq == nil {
		return nil
	}
	return &MessageWithIntsQuery{
		config:     mwiq.config,
		ctx:        mwiq.ctx.Clone(),
		order:      append([]messagewithints.OrderOption{}, mwiq.order...),
		inters:     append([]Interceptor{}, mwiq.inters...),
		predicates: append([]predicate.MessageWithInts{}, mwiq.predicates...),
		// clone intermediate query.
		sql:  mwiq.sql.Clone(),
		path: mwiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Int32s []int32 `json:"int32s,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MessageWithInts.Query().
//		GroupBy(messagewithints.FieldInt32s).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mwiq *MessageWithIntsQuery) GroupBy(field string, fields ...string) *MessageWithIntsGroupBy {
	mwiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MessageWithIntsGroupBy{build: mwiq}
	grbuild.flds = &mwiq.ctx.Fields
	grbuild.label = messagewithints.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Int32s []int32 `json:"int32s,omitempty"`
//	}
//
//	client.MessageWithInts.Query().
//		Select(messagewithints.FieldInt32s).
//		Scan(ctx, &v)
func (mwiq *MessageWithIntsQuery) Select(fields ...string) *MessageWithIntsSelect {
	mwiq.ctx.Fields = append(mwiq.ctx.Fields, fields...)
	sbuild := &MessageWithIntsSelect{MessageWithIntsQuery: mwiq}
	sbuild.label = messagewithints.Label
	sbuild.flds, sbuild.scan = &mwiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MessageWithIntsSelect configured with the given aggregations.
func (mwiq *MessageWithIntsQuery) Aggregate(fns ...AggregateFunc) *MessageWithIntsSelect {
	return mwiq.Select().Aggregate(fns...)
}

func (mwiq *MessageWithIntsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mwiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mwiq); err != nil {
				return err
			}
		}
	}
	for _, f := range mwiq.ctx.Fields {
		if !messagewithints.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mwiq.path != nil {
		prev, err := mwiq.path(ctx)
		if err != nil {
			return err
		}
		mwiq.sql = prev
	}
	return nil
}

func (mwiq *MessageWithIntsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MessageWithInts, error) {
	var (
		nodes = []*MessageWithInts{}
		_spec = mwiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MessageWithInts).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MessageWithInts{config: mwiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mwiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mwiq *MessageWithIntsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mwiq.querySpec()
	_spec.Node.Columns = mwiq.ctx.Fields
	if len(mwiq.ctx.Fields) > 0 {
		_spec.Unique = mwiq.ctx.Unique != nil && *mwiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mwiq.driver, _spec)
}

func (mwiq *MessageWithIntsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(messagewithints.Table, messagewithints.Columns, sqlgraph.NewFieldSpec(messagewithints.FieldID, field.TypeInt))
	_spec.From = mwiq.sql
	if unique := mwiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mwiq.path != nil {
		_spec.Unique = true
	}
	if fields := mwiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, messagewithints.FieldID)
		for i := range fields {
			if fields[i] != messagewithints.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mwiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mwiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mwiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mwiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mwiq *MessageWithIntsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mwiq.driver.Dialect())
	t1 := builder.Table(messagewithints.Table)
	columns := mwiq.ctx.Fields
	if len(columns) == 0 {
		columns = messagewithints.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mwiq.sql != nil {
		selector = mwiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mwiq.ctx.Unique != nil && *mwiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mwiq.predicates {
		p(selector)
	}
	for _, p := range mwiq.order {
		p(selector)
	}
	if offset := mwiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mwiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MessageWithIntsGroupBy is the group-by builder for MessageWithInts entities.
type MessageWithIntsGroupBy struct {
	selector
	build *MessageWithIntsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mwigb *MessageWithIntsGroupBy) Aggregate(fns ...AggregateFunc) *MessageWithIntsGroupBy {
	mwigb.fns = append(mwigb.fns, fns...)
	return mwigb
}

// Scan applies the selector query and scans the result into the given value.
func (mwigb *MessageWithIntsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mwigb.build.ctx, ent.OpQueryGroupBy)
	if err := mwigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MessageWithIntsQuery, *MessageWithIntsGroupBy](ctx, mwigb.build, mwigb, mwigb.build.inters, v)
}

func (mwigb *MessageWithIntsGroupBy) sqlScan(ctx context.Context, root *MessageWithIntsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mwigb.fns))
	for _, fn := range mwigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mwigb.flds)+len(mwigb.fns))
		for _, f := range *mwigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mwigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mwigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MessageWithIntsSelect is the builder for selecting fields of MessageWithInts entities.
type MessageWithIntsSelect struct {
	*MessageWithIntsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mwis *MessageWithIntsSelect) Aggregate(fns ...AggregateFunc) *MessageWithIntsSelect {
	mwis.fns = append(mwis.fns, fns...)
	return mwis
}

// Scan applies the selector query and scans the result into the given value.
func (mwis *MessageWithIntsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mwis.ctx, ent.OpQuerySelect)
	if err := mwis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MessageWithIntsQuery, *MessageWithIntsSelect](ctx, mwis.MessageWithIntsQuery, mwis, mwis.inters, v)
}

func (mwis *MessageWithIntsSelect) sqlScan(ctx context.Context, root *MessageWithIntsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mwis.fns))
	for _, fn := range mwis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mwis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mwis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
